# Changelog

## v2.7.0 - 2026-08-17
- 新增：管理员可发送或引用图片并使用 `展示 穿搭N`，为指定衣柜方案绑定展示图。
- 新增：`查看衣柜` 的 HTML 卡片展示每套穿搭的绑定图片；同一方案再次展示会原子覆盖旧图片。
- 兼容：衣柜配置迁移为带隐藏稳定 ID 的模板列表，面板重排、编辑和新增不会错配展示图片。

## v2.6.0 - 2026-08-17
- 替换：衣柜成为自动日程的唯一穿搭来源，旧 `pool.outfit_styles` 仅作为一次性迁移入口。
- 兼容：升级时将旧 `wardrobe.json` 和穿搭风格池幂等迁入面板 `wardrobe` 配置，并保持升级前的现有编号顺序。
- 调整：新增穿搭追加到衣柜末尾，不再抢占 `穿搭1`；所有方案可直接在 AstrBot WebUI 配置页增删改。
- 收紧：图片、文字和编辑转写统一使用白名单 JSON 字段，只保存服装、鞋袜、配饰、发型、妆容、造型细节和穿戴约束，丢弃场景、动作、日程、天气和镜头字段。

## v2.5.1 - 2026-08-17
- 新增：`get_life_context(allow_generate=False)` 只读取已缓存的今日日程，供图片等外部插件安全联动，缺少缓存时不会调用 LLM 生成。
- 保留：不传 `allow_generate` 时继续兼容原有的缺缓存自动生成行为。

## v2.5.0 - 2026-08-17
- 新增：`重写日程` 支持直接附图或引用图片，图片会先由可配置的视觉 provider 转写，再交给日程模型生成。
- 新增：`放进衣柜`、`查看衣柜`，支持文字与图片混合输入，并将稳定的文字穿搭方案持久化保存。
- 新增：通过 LLM tools 在自然对话中新增、删除、修改衣柜方案和今日穿搭/日程。
- 新增：图片转写 provider 可在插件配置中单独选择，留空时跟随日程 provider。
- 新增：`查看衣柜` 改为 HTML 图片卡片并显示数字编号，支持 `重写日程 穿搭N` 选择方案。
- 保留：定时日程的 `default_reference_umo` / `参考会话` 和 `{rN}` Prompt 占位符能力。

## v2.4.1 - 2026-05-24
- 修复：手动 `重写日程` 的补充要求不再被随机穿搭风格覆盖，并会校验具体穿搭和活动是否落实。
- 修复：日常对话注入当前或最近日程活动，减少回答与当天日程冲突。
- 增强：补充日程生成、当前状态注入和空 completion 重试的回归测试。

## v2.4.0 - 2026-05-20
- 新增：提供 `get_life_context()` 公共方法，供外部插件主动获取结构化日程上下文。
- 修复：`get_life_context()` 与主流程统一使用 `schedule_time` 业务日边界，避免生成时间前读错日期。

## v2.3.0 - 2026-05-20
- 新增：支持在配置中指定生成日程使用的 LLM 供应商，留空时继续使用当前 provider。
- 优化：日程读取按每日生成时间切分业务日，避免生成时间前误读新一天未生成的日程。

## v2.2.7 - 2026-02-23
- 修复：LLM 返回空 completion 时增加一次重试，并在失败时给出明确错误 `API返回的completion为空`。
- 增强：统一 completion 抽取字段（`completion_text/completion/text/content`）。

## v2.2.6
- 历史版本（详见 git 历史）。
